# Optional but recommended — raises GitHub API rate limits and unlocks
# fuller contribution history (commits across years, private totals when scoped).
# Create a classic PAT with public_repo (or fine-grained: read user + public repos).
# https://github.com/settings/tokens
GITHUB_TOKEN=

# Port for local development (default 3000)
PORT=3000
